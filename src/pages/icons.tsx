import {
  AppleIcon,
  ArrowDownIcon,
  ArrowIcon,
  AttachmentIcon,
  BuildingIcon,
  ChevronRight,
  DashboardModalCheckIcon,
  DesignIcon,
  DevelopmentIcon,
  DiscussFillIcon,
  DocumentIcon,
  DotsThreeIcon,
  DragDropFillIcon,
  ExcelIcon,
  EyeClosedIcon,
  EyeIcon,
  GoogleIcon,
  IssueIcon,
  LinkIcon,
  LockIcon,
  MoonIcon,
  NotificationFillIcon,
  OpenFolderIcon,
  PdfIcon,
  PlusIcon,
  Priority2Icon,
  PriorityIcon,
  QuestionIcon,
  SendIcon,
  ShareIcon,
  SlackIcon,
  UserPlusIcon,
  WaveFormIcon,
} from '@/icons';
import { Box, Icon } from '@chakra-ui/react';

const Icons = () => {
  return (
    <Box>
      <Icon as={AppleIcon} />
      <Icon as={GoogleIcon} />
      <Icon as={SlackIcon} />
      <Icon as={WaveFormIcon} />
      <Icon as={EyeIcon} />
      <Icon as={EyeClosedIcon} />
      <Icon as={ArrowIcon} />
      <Icon as={BuildingIcon} />
      <Icon as={DesignIcon} />
      <Icon as={DevelopmentIcon} />
      <Icon as={DiscussFillIcon} />
      <Icon as={DragDropFillIcon} />
      <Icon as={MoonIcon} />
      <Icon as={DocumentIcon} />
      <Icon as={PlusIcon} />
      <Icon as={NotificationFillIcon} />
      <Icon as={ChevronRight} />
      <Icon as={PriorityIcon} />
      <Icon as={SendIcon} />
      <Icon as={QuestionIcon} />
      <Icon as={ShareIcon} />
      <Icon as={UserPlusIcon} />
      <Icon as={OpenFolderIcon} />
      <Icon as={ExcelIcon} />
      <Icon as={PdfIcon} />
      <Icon as={LockIcon} />
      <Icon as={IssueIcon} />
      <Icon as={Priority2Icon} />
      <Icon as={DotsThreeIcon} />
      <Icon as={ArrowDownIcon} />
      <Icon as={AttachmentIcon} />
      <Icon as={DashboardModalCheckIcon} />
      <Icon as={LinkIcon} />
    </Box>
  );
};

export default Icons;
