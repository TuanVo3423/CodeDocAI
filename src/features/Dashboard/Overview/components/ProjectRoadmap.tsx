import { AIBox } from '@/ui-kit';
import { BoxHeading } from '../../components';

export const ProjectRoadmap = () => {
  return (
    <AIBox h="100%">
      <BoxHeading title="project_roadmap" isBeta />
    </AIBox>
  );
};
